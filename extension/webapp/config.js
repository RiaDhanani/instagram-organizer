// Proxy endpoints — API keys live server-side in Vercel env vars, never in the browser.
window.IG_CONFIG = {
  categorizeUrl: 'https://saved-posts-organizer.vercel.app/api/categorize',
  webSearchUrl: 'https://saved-posts-organizer.vercel.app/api/websearch',
};
